@model Web.ViewModels.NaturezaOperacaoViewModel

@{
    ViewData["Title"] = "Excluir Natureza Operação";
}

<h3>@ViewData["Title"]</h3>

<h5>Confirma a Exclusão do Registro?</h5>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Id)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Id)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Descricao)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Descricao)
        </dd>
    </dl>

    <form asp-action="DeleteNatureza">
        <input type="hidden" asp-for="Id" />
        <a asp-action="NaturezaOperacaoIndex" class="btn btn-primary">Voltar</a>
        <input type="submit" value="Excluir" class="btn btn-danger" />
    </form>
</div>
